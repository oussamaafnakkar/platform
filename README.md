# 📚 منصة التقويم المستمر
**المدرسة العليا للتربية والتكوين**

---

## 🗂️ هيكل المشروع

```
📁 المشروع/
├── index.html                  ← الصفحة الرئيسية
├── google-apps-script.js       ← كود Google Sheets (مرة واحدة فقط)
├── exam-1/
│   ├── index.html              ← لا تعدّل
│   ├── style.css               ← لا تعدّل
│   ├── script.js               ← لا تعدّل
│   └── questions.js            ← ✏️ الوحيد الذي تعدّله
└── exam-2/
    ├── index.html              ← لا تعدّل
    ├── style.css               ← لا تعدّل
    ├── script.js               ← لا تعدّل
    └── questions.js            ← ✏️ الوحيد الذي تعدّله
```

---

## ✅ ما يجب تعديله (مرة واحدة فقط)

### الخطوة 1 — Google Apps Script (مرة واحدة للأبد)

1. افتح Google Sheet جديد على [sheets.google.com](https://sheets.google.com)
2. **Extensions → Apps Script**
3. احذف كل شيء، الصق محتوى ملف `google-apps-script.js`
4. احفظ **(Ctrl+S)**
5. **Deploy → New Deployment**
6. Type: **Web app**
7. Execute as: **Me**
8. Who has access: **Anyone**
9. اضغط **Deploy** → **Authorize** → امنح الصلاحيات
10. **انسخ الرابط** (يبدأ بـ `https://script.google.com/macros/s/...`)

> ⚠️ بعد هذه الخطوة **لن تحتاج لفتح Apps Script مرة أخرى أبداً**.
> يعمل تلقائياً 24/7 مجاناً إلى الأبد.

---

### الخطوة 2 — ضع الرابط في questions.js

افتح `exam-1/questions.js` و `exam-2/questions.js`
عدّل هذا السطر فقط في كل منهما:

```js
sheetURL: "https://script.google.com/macros/s/YOUR_ID/exec",
```

وكذلك يمكنك تغيير كلمة المرور:
```js
password: "كلمة_المرور_الجديدة",
```

---

### الخطوة 3 — نشر على GitHub Pages

```bash
git add .
git commit -m "ready"
git push
```

ثم في GitHub:
**Settings → Pages → Branch: main → Save**

رابطك سيكون:
`https://username.github.io/repo-name/`

---

## 🔍 للتحقق أن كل شيء يعمل

افتح هذا الرابط في المتصفح مباشرة:
```
https://script.google.com/macros/s/YOUR_ID/exec
```
يجب أن ترى: `{"status":"ok","message":"يعمل بشكل صحيح ✓"}`

---

## 🆕 إضافة تقويم جديد

1. انسخ مجلد `exam-2` → `exam-3`
2. عدّل `questions.js` فقط (title + subject + QUESTIONS)
3. أضف بطاقة في `index.html` الرئيسية
4. نفس `sheetURL` — كل شيء يذهب لنفس Google Sheet

---

## 📊 Google Sheet — الأوراق التلقائية

بعد أول إرسال ستجد 3 أوراق تُنشأ تلقائياً:

| الورقة | المحتوى |
|--------|---------|
| **النتائج** | جميع النتائج ملونة حسب الدرجة |
| **الأسئلة المفتوحة** | إجابات تُصحَّح يدوياً |
| **الطلاب** | قائمة الطلاب + عدد الامتحانات |

